development: &defaults
  reconnect: true
  adapter: postgresql
  username: root
  password:
  database: nightout_development
  pool: 20

test:
  <<: *defaults
  database: nightout_test

production:
  <<: *defaults
